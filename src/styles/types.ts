export interface CustomTheme {
	colors: {
		buttonBgPrimary: string;
		buttonBgDisabled: string;
		buttonTextDisabled: string;
		textPrimary: string;
	};

	order: {
		mainIndex: number;
		firstIndex: number;
		mediumIndex: number;
		lastIndex: number;
	};
}
